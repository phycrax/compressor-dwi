[workspace]
resolver = "2"

members = [
    "compressor-dwi",
    "secop-dwi",
]

[workspace.dependencies]
defmt               = { version = "1.1" }
embedded-hal-async  = { version = "1.0" }
embedded-io-async   = { version = "0.7" }
